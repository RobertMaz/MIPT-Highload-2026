# Deployment Document: <Название системы>

## 1. Архитектура

Назначение: <одно-два предложения о том, что делает система и для кого>

Нагрузка:

- <профиль нагрузки на ключевые операции — RPS, RPM>
- <пиковые сценарии, если есть>

Сервисы:

- <Сервис A> ×<N в каждом AZ> — <ответственность>, <stateless / stateful>, <public / private zone>
- <Сервис B> ×<N> — ...
- ...

> Канонический пример C4 deployment diagram: https://c4model.com/diagrams/deployment

---

## 2. Observability

Алерты (4, не больше) — только Golden Signals на critical path:

| Сигнал     | Метрика                                | Порог                    | На что            |
|------------|----------------------------------------|--------------------------|-------------------|
| Latency    | <метрика, например p99 POST /endpoint> | <порог + временное окно> | <сервис>          |
| Errors     | <метрика>                              | <порог>                  | <сервис>          |
| Traffic    | <метрика>                              | <порог>                  | <сервис>          |
| Saturation | <метрика — pool, queue, connections>   | <порог>                  | <сервис / ресурс> |

Дашборды (3 уровня):

- Overview — Golden Signals на всю систему (1 экран)
- Service-level — RED по каждому сервису отдельно
- Diagnostic — USE на ресурсы + traces по запросу

Логи:

- Формат: <JSON в stdout / другое — обоснуйте>
- Обязательные поля: <trace_id, level, service, msg, ...>
- Что логируем: <входящий request, исходящий call, ошибка со stack trace>
- НЕ логируем: <PII, секреты>
